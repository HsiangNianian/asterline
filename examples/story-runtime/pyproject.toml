[project]
name = "story-runtime"
version = "0.1.0"
description = "Story-driven Asterline example."
requires-python = ">=3.10"
dependencies = [
    "asterline",
]

[tool.uv.sources]
asterline = { workspace = true }
